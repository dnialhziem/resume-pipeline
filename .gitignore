# Personal data — never commit this
resume/data.json

# User photo — add locally, never commit
resume/photo.*
resume/*.jpg
resume/*.jpeg
resume/*.png

# Python
__pycache__/
*.pyc
*.pyo
.env

# OS
.DS_Store
Thumbs.db

# Output PDFs — generated by CI, not stored in repo
resume/*.pdf
